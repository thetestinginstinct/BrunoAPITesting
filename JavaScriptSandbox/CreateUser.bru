meta {
  name: CreateUser
  type: http
  seq: 2
}

post {
  url: https://reqres.in/api/users
  body: json
  auth: none
}

body:json {
  {
      "name": "morpheus",
      "job": "leader"
  }
}

tests {
  const { assertCodes } = require("../shared-scripts/utils");
  
  test("Status code should match expected value", () => {
    expect(res.status).to.equal(assertCodes[1]);
  });
}
