meta {
  name: Basic Auth
  type: http
  seq: 3
}

get {
  url: https://postman-echo.com/basic-auth
  body: none
  auth: basic
}

auth:basic {
  username: postman
  password: password
}

tests {
  const { assertCodes } = require("../shared-scripts/utils");
  
  
  test("Status code should match expected value", () => {
    expect(res.status).to.equal(assertCodes[0]);
  });
}
